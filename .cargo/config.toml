[alias]
t = "nextest run --workspace --all-features --locked"
e2e = "nextest run --workspace --all-features --locked --profile e2e --run-ignored only"
live = "nextest run --workspace --all-features --locked --profile live --run-ignored only"
probes = "nextest run --workspace --all-features --locked --profile probes --run-ignored only"
