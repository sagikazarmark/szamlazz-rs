# Storno email contract review — #223

Baseline: `99480de4bbcd3344aa4cdc7438d11c378e4691c6`, reviewed against the working
tree before commit. Independent Standards and Spec reviews, followed by focused
verification of their fixes.

## Standards

- **P3, resolved:** pre-send probe diagnostics omitted the storno external id
  required by `docs/testing.md`. The diagnostic now records it before sending.
- **P3 heuristic, resolved:** repeated intent-to-step projections risked forwarding
  drift. `StornoIntent::as_step_request` now supplies preflight and both storno
  execution paths; unmanaged execution captures the cloned intent.

Follow-up review found no remaining Standards findings.

## Spec

- **P2, resolved:** an unmanaged numbered code-56 reply could lose its notification
  warning when by-number verification failed and immediate external-id reconciliation
  established the same storno. A red/green public Gateway regression reproduces it.
  The Gateway now preserves that acknowledged document and warning only when the
  fallback establishes the exact same number; other outcomes keep their prior
  classification. No additional mutation is sent.

Follow-up review found no remaining Spec findings. Historical notification
information unavailable to queries remains unavailable, as documented.

## Verification

- `cargo test --workspace --all-features --locked --quiet`: passed, including doctests.
  The first full run exposed two existing classification expectations; the fix was
  narrowed to reported-warning cases and the complete suite then passed.
- Focused public contract/schema and Gateway tests: passed; red-before-green recorded
  for optional input, forwarding, caller warning and immediate reconciliation.
- Two actual-Restate 1.7.8/mocked-provider `e2e_storno_email_` tests: passed after the
  shared-intent refactor, covering both handlers, invalid input before prologue,
  omission/explicit wire values, managed routing, completed replay and protected
  uncertainty through pause/resume/later invocation without a second send.
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`,
  `cargo fmt --all --check`, `git diff --check`: passed.
- Worker checks with no default features and with `schemars`: passed. Full
  cargo-hack feature powerset was not run (`cargo-hack` unavailable).
- `cargo xtask check-agent-schemas`: passed (430 generated requests, 790 valid
  source/case verdicts, 70 documented source conflicts, seven negative controls).
  All eight `schema_runner --include-ignored` regressions passed.
- `dagger call ci live --help`: passed, exposing `--storno-email`; no extra live
  operation was run by this check. Full Dagger checks/all ignored e2e scenarios
  were not run for this change.
- Four opt-in vendor probes ran once; all originals and reversals were verified.
  The operator reported both explicit-recipient emails received. See the
  [dated evidence and limitations](../research/2026-09-12-storno-email-delivery.md#controlled-live-execution-2026-09-12).

Totals: Standards 2 initial findings (one heuristic), 0 remaining; Spec 1 initial
finding, 0 remaining.
