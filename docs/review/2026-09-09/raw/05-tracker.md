# Issue-tracker review, `szamlazz-rs`, 2026-09-09 (before the edits)

Research only at the time of writing; every "edit body" action below was then applied with the review (see `REVIEW.md` §6). Verified against code with grep/read and against the merged PRs, not from PR titles.

## Per-issue verdicts

Verdict: **a** still valid as written · **b** partially done / needs update · **c** obsolete / closable · **d** blocked-by edge points at a closed issue.

| # | Title (short) | Verdict | Evidence | Recommended action |
|---|---|---|---|---|
| **45** | Ops runbook, alerting, ADR 0004 amendments | **b** | No `docs/runbook.md`; no `event` field on any `warn!` (`src/gateway.rs:923…1577`). Item 4 is **half done** by ADR 0004 `## Amended (#87)` (lines 198–247: run retries do not spend invocation attempts, iterator cumulative over lifetime, abort timeout counts); *not* recorded: `retry_count`/`last_failure` vanishing on the vqueue-yield path, alerting on vqueue tables. Item 5 half done: `tests/e2e/get.rs:82` asserts `max_retry_count > get_max_attempts` (no vacuous `>= 1`); no vqueue-table read. Item 2 names `account_mismatch`, `mode`/`supplier_id`, the "wrong-`supplier_id`-on-a-fresh-account trap": all removed by ADR 0006's account-pin amendment (PR #107). Blocker #37 closed. | edit body: strike the `account_mismatch`/`mode`/`supplier_id` procedures; mark item 4's independence/lifetime/abort-timeout facts done (ADR 0004 #87), keep the vqueue half; mark the `>= 2` assertion done; drop *Blocked by #37*; list the #65 span fields (`execution{scope, order, restate.invocation.id, account.id}`) in item 3. |
| **50** | `get` runs four reads concurrently | **a** (tabled) | Still sequential: `src/service/storno.rs:174-185` loops `DocumentKind::ALL`. The tabling comment (SDK 0.12 `RunFuture` cannot join without 570 JOURNAL_MISMATCH) still holds. Stale line refs only: body cites `storno.rs:221` / `shared::lookup` (now `support::lookup` over `RunCtx`, PR #172); comment cites `tests/service.rs:2558` (now `tests/e2e/get.rs`). | keep (or close as "tabled until sdk-rust registers the run command eagerly"); optional one-line comment refreshing the file:line refs. |
| **59** | Whole-workspace review 2026-09-06 tracking | **b** | Every restate/agent row closed (#60–#67, #69–#73); #77 and #46 closed by PR #169 (endpoint removed) but the table still lists #77's "tail: J-07-06/07/08, J-05-15, J-07-19/21/22"; #14/#15/#44 closed. Open descendants: #68, #74, #75, #76, #45 only. | edit body: strike #77 row ("closed with #169, endpoint crate removed; J-05-15 done by #128"), mark #14/#44/#15 done, note the four open umbrellas + #45 as the remainder; or close as tracking-complete pointing at those five. |
| **68** | restate-szamlazz refactor backlog | **b** | J-05-05 line says "The macro **stays** (#133 won't do)": stale; PR #172 (`9ab860e`) replaced `journal_helpers!` with `trait RunCtx` (`src/service/support.rs:578`); no `journal_helpers` remains in `src/`. The 2026-09-08 18:01 comment ("done by #133, commit 12e3886, `service::runner::Runner`, `FakeRunner`, `service::paths`, `src/service/run_names.rs`") describes a branch never merged: none of those exist on `main`, and #133 closed *won't do* at 19:04. Still open: the split by concern; `support.rs` is 993 lines holding the `Journaled` registry, `Fault` constructors, storno decisions (`StornoIntent`, `after_storno_lookup`), `Lookup::classify`, `RunCtx` + 13 run helpers. J-05-07/J-05-08 parent box unticked though both children closed. J-05-14 → #116 closed (tree exists: `tests/e2e/`). AC "fixture generator byte-identical, or archives a shape": no fixtures exist (ADR 0009). *Blocked by #66*: closed. | edit body: rewrite J-05-05 ("macro gone via `RunCtx` in #172; open: split `support.rs` by concern"), tick J-05-07/08 and J-05-14, delete the fixture AC, drop #66; add a correcting comment that the 18:01 comment described an unmerged branch. |
| **74** | adatkapcsolat findings umbrella | **b** | Remaining rows verified live: J-07-14 (`src/axum.rs:355,380` echo `error.to_string()` in 400 bodies), J-05-09 (four `router*` fns `axum.rs:175-220`), B-08 (`ack.rs:74` infallible), B-18 (401 without `WWW-Authenticate`, `axum.rs:342`). Not reflected: J39 fixed (#66 `--all-features`, `.dagger/modules/ci/main.dang` `test`), J-05-01 → #129, B-22 → #130 item 3, B-03's logging half → #130 item 2, B-19's residual → #148 (comment says so). #122 done (#160). | edit body: tick J39; link J-05-01→#129, B-22/B-03→#130, B-19→#148, B-17/B-15 etc. remain; strike the "top-ranked" B-04/B-01 table (all done). |
| **75** | ipn findings umbrella | **a** | B-09: README line 39 has the defence-in-depth sentence but not "confirm `paid_gross` via the Agent before acting"; B-10 → #149 (still `payment_method: String`, `lib.rs:111`); B-11/B-12 undocumented. | keep; tick "B-10 is a ticket" (→ #149). |
| **76** | cli findings umbrella | **a** | `listen` default `127.0.0.1:8080` (`commands/listen.rs:23`) vs `compose.yaml:8`; no `reverses()` in `commands/`; `taxpayer` takes `tax_number_prefix` only; `agent_key: Option<String>` (`main.rs:22`); zero tests (no `#[test]`, no `tests/`). R08-CLI → #150. | keep; tick R08-CLI as ticketed (#150). |
| **106** | `Szamlazz.OrderHistory` (parked) | **b** | Premise intact (`get` reads newest holder only). Stale: "endpoint README, *What to keep*": crate removed (#169); the library README's equivalent is *Scope Contract* item 5 / *Caller Contract* (`README.md:162, 462`). Open question 5 ("additive-only like every Journaled type… does `service::journal` need a fixture") is void under ADR 0009: no additive-only rule, no fixtures; a run result needs a `journaled!` entry + sample, a `OneWayCall` input is outside the sealed bound and covered by the e2e raw scan only. "Run-name pin" → *Step-name table*. | edit body: repoint the README reference; rewrite question 5 in ADR 0009 terms. |
| **109** | Fixture drift check | **a** | No `fixtures/upstream/SOURCES.toml`, no `.github/workflows/fixture-drift.yaml`; `SOURCES.md:124` still cites `3fc523a`. #72 landed (corpus load-bearing). | keep. |
| **111** | XSD validation of request XML / synthetic fixtures | **a** | `crates/szamlazz-agent/tests/upstream` symlink present; no `xmllint`/`libxml`/xsd validation in any test. | keep. |
| **112** | Undecodable `<pdf>` fails the document? | **b** | Behaviour unchanged (`ops/query_xml.rs:586` `Pdf::from_base64(&encoded)?`; `tests/upstream.rs:421 refused_as_base64`). Stale paragraph *The worker's stake*: the worker no longer journals `InvoiceDocument` (PR #165: `FoundDocument`, `src/gateway/document.rs`, carries no `pdf`), and "additive-only" is gone (ADR 0009); the point that a refusal becomes an *Unanswered* read → `unavailable` still holds. | edit body: rewrite the worker paragraph (journal shape cannot be affected; only the Unanswered/unavailable consequence remains). |
| **121** | Whole-workspace review 2026-09-08 tracking | **b** | Table statuses stale: #123 (done PR #164), #125 (PR #163), #127 (PR #165), #128 (PR #168), #134 (PR #171) closed but not struck; #133 row says "the helpers stay" (superseded by #172's `RunCtx`). *Preserve* names "the journal fixtures, the run-name pin": fixtures removed by ADR 0009, "pin" retired (CONTEXT.md:235). Open: #126, #129, #130, #131, #132. | edit body: strike the five done rows with their PRs; update the #133 row; reword *Preserve* (leak scan complete by mechanism + step-name table). |
| **126** | Property tests; client timeout | **a** | No `proptest`/`quickcheck` in `Cargo.lock`; `tests/client.rs` builds with `REQUEST_TIMEOUT` but no stall test. | keep. |
| **129** | Integer-width policy, typed `tipus`, shared `<szamla>` | **b** | Divergences verified (`query_xml.rs:198 id: u64` vs `document.rs:423 Option<i64>`; `location i64` vs `i32`; two `InvoiceAppearance`). Stale AC: "worker's journal fixtures unchanged … or archived deliberately": no fixtures (ADR 0009); since #165 no agent type is journaled, the worker projects `FoundDocument { document_type: String, appearance: i32 }` (`gateway/document.rs:55,77`), so the width change touches the journal only through that projection. Overlaps #139 on item 2. | edit body: replace the fixture AC with "the `FoundDocument` projection decides what the journal carries"; note #139 is the `tipus` slice. |
| **130** | adatkapcsolat 0.4 API items | **a** | `axum.rs:68` still `Option<&Self::Handler>`; `ack.rs:63 accept(id: i32)`; no `subtle`/`tracing` in Cargo.toml; `keys_match` private (`axum.rs:454`). | keep. |
| **131** | Rust/rustdoc hygiene batch | **b** | Still open: `From<quick_xml::DeError>` (`agent/error.rs:574`, `adatkapcsolat/error.rs:62`), `From<base64::DecodeError>` (`:580`), `rust_decimal` `macros` on the workspace dep (`Cargo.toml:34`), four bare `#[allow]` (`invoice.rs:857`, `query_xml.rs:193,735,1387`), `Intent { create: CreateInvoice }` (`create.rs:185-187`), stale `teszt == mode` (`query_xml.rs:263`), IPN datetime test (`ipn/lib.rs:385`). **Largely done**: the "hundred rustdoc history lines"; commit `893167c` and #172 removed ADR/design refs; a few issue-number lines remain in `src/`, most in `service/tests.rs`. | edit body: shrink the *history out of rustdoc* bullet to the residual; rest stands. |
| **132** | nextest profiles, MSRV check, no pass-by-skipping | **b** | No `.config/nextest.toml`, no `.cargo/config.toml` aliases; no MSRV check (`dagger.toml` container `1.98-slim-trixie`, `rust-version = "1.92"`); `adatkapcsolat/tests/document.rs:655` still prints `skipped`. Stale: the `figment::Jail` hazard and "binary-spawning endpoint tests" (endpoint crate removed, #169); the Dagger comment now reads "archiver tests (opendal) and the `schemars` contract tests" (`main.dang` `test`); "hung `/health` loop 90 s" refers to the old harness (now `restate-e2e-harness`, free ports). Blocker #123 closed. | edit body: drop the endpoint/figment clauses, re-check the Dagger-comment item against `main.dang`, drop *Blocked by*. |
| **135** | Architecture-and-tests review tracking | **b** (minor) | Rows for #136/#137/#138/#140/#142/#143/#152 correctly struck. Stale: #144 row "the endpoint binary compiled once"; #153 row "journal layout unchanged"/#139 "journal layout unchanged" premise; #156 row says "eszamla = 1 everywhere" still to do (correct). | edit body: trim the #144 row; note #141 partly done by #171. |
| **139** | Typed `DocumentType` for `tipus` | **b** | Literals remain: `gateway.rs:1674-1700`, `service/storno.rs:269`, `service/create.rs:368`; agent `document_type: String` (`query_xml.rs:209`). Stale: "worker's journaled `InvoiceDocument`"/"journal fixture compatibility test proves it" (now `FoundDocument`, no fixtures; ADR 0009), "behind `InvoiceDocumentExt`" (trait gone since #165; the methods are `FoundDocument`'s, `gateway/document.rs`). | edit body: retarget to `FoundDocument::document_type`, drop the fixture AC. |
| **141** | e2e: every scenario reports, keys, kill premise, Reuse, `RUN_NAMES` = discovery, archive | **b** | Done by #171: phase-1 collects every failure (`tests/e2e/main.rs:111-168 Concurrently::join_all`), scenarios own keys (`E2E-1` shared only deliberately by the flag-day read), kill premise is the held fetch (`prologue.rs:107-115 hold_fetch`). Not done: phase 2 is `sequentially!` (first failure aborts the rest); Reuse mode still replays literal keys (`"e2e-12-k1"`, `pins.rs:43`; nothing salts); `RUN_NAMES` vs discovered handlers not asserted (`pins.rs:203-205` marks unpinned only when an invocation exists; `service/tests.rs:56,151` discovery tests do not cross-check). **Obsolete**: the journal-fixture archive bullet (ADR 0009). | edit body: tick the three done items, delete the archive bullet, narrow to phase-2 reporting + Reuse salting + `RUN_NAMES`=discovery. |
| **144** | Debuginfo profile; endpoint binary once | **b** | No `[profile.dev]` in `Cargo.toml` (only `profile.dist`). Endpoint half moot (#169; the comment already says so). | edit body: fold the comment in (drop AC 3, re-measure on the current workspace). |
| **145** | Envelope parser leaves `ops/invoice.rs` | **a** | `ops/invoice.rs` 2238 lines; `parse_issued` at `invoice.rs:1295`, imported by `storno.rs:8`. | keep. |
| **146** | Operation × failure-shape table; rounding edges | **a** | `wire.rs` tests are per shape, not per operation; `item.rs:435` only `Scale(4)`. | keep. |
| **147** | Body-limit tests stop copying 64 MiB | **a** | `tests/protocol.rs:643,687` build 64 MiB+1 twice. | keep. |
| **148** | Feature-free parse tests, lexical cases, `keys_match`, fixture module, README doctest | **a** | `tests/protocol.rs:4 #![cfg(feature = "axum")]`; `keys_match` has 0 tests; no `ReadmeDoctests` in adatkapcsolat `lib.rs`. | keep. |
| **149** | ipn content lenient | **a** | `payment_method: String` (`lib.rs:111`); `payment_date_requires_the_documented_date_only_format` still asserts a refusal (`lib.rs:385`). Overlaps #131's IPN bullet. | keep; cross-link #131. |
| **150** | cli first tests | **a** | No tests in `szamlazz-cli`. | keep. |
| **151** | One TLS config per process behind `Gateway::open` | **b** | `Gateway::open` still builds a default `Client` per open (`gateway.rs:822-828`); `open_with_http` exists (#157). Stale body: "the container image installs `ca-certificates`", "the endpoint" as a home, "endpoint README" AC; the comment already amends all three. Blocker #136 closed. | edit body: fold the comment in; drop *Blocked by*. |
| **153** | One answered-code → fault mapping; rustdoc drift | **b** | Mapping still per site: 12 `CredentialsRejected(SzamlazzAnswer)` variants (`gateway.rs:309…1730`), 14 non-test `Fault::credentials_rejected` calls, 8 `inconclusive_answer`. Rustdoc drift **half fixed**: crate root and contract module now say "three of the seven" (`lib.rs:175`, `contract.rs:6-9, 77-80`); **still stale**: `gateway.rs:18` "ownership-validation pins", `:257` and `:321` "validated against the gateway's own `Account`". No doctest pins the three codes. **Obsolete**: "journaled enums stay byte-identical (additive-only)" and AC "journal fixtures byte-identical" (ADR 0009; PR #168 already unified the variants to `SzamlazzAnswer`). Blockers #137/#138 closed. | edit body: drop the byte-identical/fixture clauses, narrow the rustdoc item to the three `gateway.rs` lines, drop *Blocked by*. |
| **154** | `build_create` split; typed storno id constructors | **b** | `build_create` (`gateway/build.rs:83`), `validate_document` with `ExternalId::new("-")` placeholders (`service/create.rs:607-624`), `for_storno(&str)` (`identity.rs:771,781`), `normalize_buyer_name` in `identity.rs:863`: all as described. Stale AC: "journal fixtures byte-identical". Blocker #137 closed. | edit body: drop the fixture AC and *Blocked by*. |
| **155** | `E2E_ONLY` filter | **a** | No `E2E_ONLY` anywhere. Blocker #141 still open and still the right edge (families own keys; that part of #141 is done, so this is effectively unblocked). | keep; note in body that the key-ownership prerequisite landed in #171. |
| **156** | `tests/gateway/` tree; classification tests to unit; renderers default to paper | **b** | Still one 2769-line `tests/gateway.rs`. **Done**: classification tests moved to unit (`tests/gateway.rs:11-16` module doc; #161), credential-code loop collapsed to one table (`:1478`); "three `Doc` renderers" → **one** shared `tests/common/mod.rs` included by path (`src/test_support.rs:48-50`, `tests/e2e/main.rs:76-78`; #171). **Open**: `eszamla` default still `2` (`tests/common/mod.rs:22, 100-103` explicitly says "#156 moves the default to `1`"); `unconfirmed_displays_name_their_cause` display test still in the integration file (`:1020`); the tree split. **Obsolete**: "journal fixture… regenerates byte-identical or archives deliberately" (ADR 0009; `service/journal.rs:405` sample has `eszamla 2` and just needs editing). Blocker #136 closed. | edit body: tick classification-to-unit and one-renderer; drop the archive clause; scope to tree split + display test + `eszamla` default `1`; drop *Blocked by*. |

**Blocked-by edges (d):** every remaining edge points at a closed issue and should be removed: #45→#37, #132→#123, #151→#136, #153→#137/#138, #154→#137, #156→#136, #68→#66. Only #155→#141 is live (and #141's relevant half is done).

## Tracking issues: checklist state

- **#59** (2026-09-06): all PR-sized rows closed (#60–#67, #69–#73). Umbrella row #77 is closed by #169 but still listed with an open "tail"; #14/#15/#44 shown as "recorded on" are closed. Open remainder: #68, #74, #75, #76, #45. The body was never ticked because it uses tables, not checkboxes; a one-paragraph status ("what remains") is missing.
- **#68**: 12 items ticked correctly; J-05-07/08 parent and J-05-14 (→ #116, closed) should be ticked; J-05-05 text is wrong twice (the macro is gone via `RunCtx`, not "stays"; the 18:01 comment reports an unmerged `Runner` branch). AC 2 (fixtures) is void under ADR 0009.
- **#74**: AC 1 ticked correctly; the remaining-rows table does not link the descendants that now exist (#129 for J-05-01, #130 for B-22/B-03, #148 for B-19, #147) and J39 is fixed but unmarked.
- **#75** / **#76**: ACs untouched though B-10→#149 and R08-CLI→#150 exist; otherwise accurate.
- **#121**: five closed tickets (#123, #125, #127, #128, #134) not struck; #133 row and *Preserve* paragraph predate #172/ADR 0009 (fixtures, "run-name pin").
- **#135**: struck rows match the closed set exactly; the only drift is the #144 row's endpoint half and #141 being partly delivered by #171.
